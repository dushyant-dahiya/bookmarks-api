services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: bookmarks_user
      POSTGRES_PASSWORD: bookmarks_password
      POSTGRES_DB: bookmarks_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: